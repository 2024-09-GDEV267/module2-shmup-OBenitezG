using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Deck : MonoBehaviour
{
    // List Deck = []


    // Constructor()
    // for every colour in colour enum
    //  for every value in range(inclusive 1 - 10)
    //      
    //      if value == 1 {
    //          for agreement in range 3
    //              Card card = new Card(colour, value)
    //              deck.append(card) 
    //      else {
    //          Card card = new Card(colour, value)
    //          deck.append(card)

    // Call shuffle method


    public void Shuffle()
    {
        // deck = Random.Shuffle(deck)
    }




    // Start is called before the first frame update
    void Start()
    {
        
    }


}
